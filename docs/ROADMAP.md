---
title: Roadmap
updated: 2026-04-22
status: current
domain: product
---

# poki soweli — Roadmap

Single source of truth for what's done, what's partial, what's open, and the Definition of Done for the current v0.2 release-hardening milestone. Every task has a stable ID (`T<phase>-<n>` or `V5-<n>` for RPG.js-v5-specific items); commit messages and PRs reference the ID.

**Branch policy:** do not assume a fixed branch name; check `git branch --show-current`.

**Status legend:**

| Mark | Meaning                                                                                                                        |
| ---- | ------------------------------------------------------------------------------------------------------------------------------ |
| ✅   | Done or explicitly superseded — behavior observable in playtest, or the old requirement was retired with a proven replacement. |
| 🟡   | Partial — contract met, polish or runtime wiring still open.                                                                   |
| ⬜   | Open — nothing landed yet.                                                                                                     |

---

## Definition of Done — current v0.2 release

Ships when **all** of these are objectively true in a fresh-install playtest on both web and Android debug:

1. **Full playthrough, softlock-free.** Boot → title → new game → `ma_tomo_lili` → jan Sewi ceremony (pick 1 of 3) → walk `nasin_wan` → catch ≥ 1 wild creature → defeat jan Ike rival → progress through all 7 beats → beat green dragon → credits. No dead-ends, no unreachable content.
2. **Catch count reachable.** Can catch ≥ 20 distinct wild creatures across the roster without grinding.
3. **Combat is legible and fair.** Action-battle HP indicators, damage flash, type matchups visibly change damage (color + label), victory sequence shows XP → level-up → new-move toasts, defeat returns to last village with party preserved at full HP.
4. **UI is themed and mobile-legible.** All overlays use BRAND.md tokens (palette + typography + spacing). Touch targets ≥ 44 dp on a 1080p phone. sitelen-pona glyphs (via UCSUR + emoji fallback) render in creature names, moves, badges.
5. **Saves and resumes.** Quit from anywhere → relaunch → Continue restores party, inventory, flags, map, tile, encounter log, mastered-words. Autosave on map change, combat end, and quit.
6. **Content volume.** All 7 beats playable, 43 species + 17 moves + 5 items wired. 4 jan lawa fights + 1 rival + 1 green-dragon endgame set-piece, plus the current 7 side quests. ≥ 80 validated multi-word TP lines in-game.
7. **Audio live.** BGM cross-fades by map/combat context (town / forest / water / ice / peak / endgame / combat). ≥ 12 SFX wired (see T5-04). Settings sliders control Music + SFX buses.
8. **Exports green.** Android debug APK produced on every PR (`ci.yml`) and release-please tag (`release.yml`); web HTML5 ≤ 10 MB gzip, deployed to GitHub Pages via `cd.yml`. Signed release APK production remains outside the current release contract.
9. **Tests carry their weight.** Integration suite covers the full golden path (boot → ceremony → warp → encounter → catch → gym → respawn → save round-trip). Unit suite covers pure-logic invariants. Both projects green on every PR.
10. **release-please tag created** with CHANGELOG drawn from Conventional Commits on `main`.

Everything beyond this is `v0.3.0` material.

---

## Phase inventory

| #   | Phase                        | Outcome                                                                                     | Done | Partial | Open |
| --- | ---------------------------- | ------------------------------------------------------------------------------------------- | ---- | ------- | ---- |
| 1   | Playable Vertical Slice      | Fresh game → starter → encounter → warp → return, no softlocks                              | 14   | 0       | 0    |
| 2   | Combat Polish + Party        | Combat UI themed, HP bars animate, party panel, catch UX, XP/level                          | 15   | 0       | 0    |
| 3   | Save, Menus, Transitions     | Title, pause, settings, continue, autosave, loading screens                                 | 13   | 0       | 0    |
| 4   | Content Breadth              | 7 regions biome-correct, 4 jan lawa set-pieces, badge gates, bestiary                       | 17   | 0       | 0    |
| 5   | Audio, Mobile, Accessibility | Music per region, SFX wired, mobile HUD + tap-to-walk, AA contrast, text speed              | 16   | 0       | 0    |
| 6   | Release Hardening            | Debug APK release artifacts, web ≤ 10 MB, CI matrix, meaningful tests, current v0.2 release | 14   | 2       | 2    |
| 7   | Post-v0.2 Depth + Replay     | Journey beats 8-10, status effects, breeding, NG+, secret areas                             | 1    | 6       | 3    |
| 8   | Language Learning Layer      | Sentence log, sitelen glyphs, micro-game, dictionary export                                 | 6    | 0       | 0    |
| 9   | Engine, Perf, Infrastructure | Asset compression, bench suite, error tracking, visual regression                           | 3    | 0       | 7    |
| 10  | Platform Expansion           | iOS, Play Store listing, desktop shells, WebXR                                              | 0    | 0       | 5    |

"Done" counts the items marked ✅. "Partial" counts 🟡. "Open" counts ⬜. Totals below assume every sub-task in every phase, including the two parallel tracks in Phase 6 (E2E-first test extension + release plumbing). RPG.js-v5-specific compatibility items are tracked separately below and are not included in the phase inventory.

---

## Phase 1 — Playable Vertical Slice ✅

Playable today. The original vertical slice now has starter selection, first route encounter paint, forest art pass, rival progression, warp gating, combat return, XP, flee, and respawn coverage.

| ID    | Title                                          | Notes                                                       |
| ----- | ---------------------------------------------- | ----------------------------------------------------------- |
| T1-01 | Starter ceremony dialog triggers end-to-end    | ✅                                                          |
| T1-02 | Pick from 3 starters (seli/telo/kasi)          | ✅                                                          |
| T1-03 | Read-from-party (not hardcoded) in encounters  | ✅                                                          |
| T1-04 | Populate `nasin_wan` encounter table           | ✅ spec-authored                                            |
| T1-05 | Tall-grass tile painting in `nasin_wan`        | ✅ spec `nasin_wan.ts` Encounters layer                     |
| T1-06 | jan Ike set-piece rival                        | ✅                                                          |
| T1-07 | FieldTriggerWatcher for non-warp triggers      | ✅ RPG.js v5 `onInShape` — free                             |
| T1-08 | Combat → field return                          | ✅ `@rpgjs/action-battle` — free                            |
| T1-09 | XP + level-up on victory                       | ✅ pure helper shipped (see T2-07); runtime wiring done     |
| T1-10 | Faint → whiteout → restore at last village     | ✅                                                          |
| T1-11 | Flee action in combat                          | ✅ wild only — action-battle jan lawa fights are real-time melee |
| T1-12 | Forest tileset for `nasin_wan`                 | ✅ seasons forest tileset + route paint landed under T4-02  |
| T1-13 | Tileset mapping w/ walkable + tall_grass flags | ✅                                                          |
| T1-14 | Sanity playthrough checklist                   | ✅ superseded by this doc                                   |

---

## Phase 2 — Combat Polish + Party Panel

**Goal:** combat no longer looks like a debug harness. Player sees whose turn it is, how much damage they did, and manages their roster from the field.

| ID    | Title                                              | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----- | -------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T2-01 | Theme combat UI (panels, fonts)                    | ✅     | The action-battle hotbar now inherits the poki brand tokens through `src/styles/brand.css`, with mobile-safe sizing, selected/disabled states, and no Vue override.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| T2-02 | Animate HP bars (tween, color shift)               | ✅     | Live action-battle combatants render `poki-combat-hp-bar.ce`, backed by `src/modules/main/combat-chrome.ts`; HP tiers, TP labels, and colors come from `src/content/gameplay/visuals.json`; set-piece enemies carry explicit `MAXHP`; and a local requestAnimationFrame tween drives width + color transitions. The pure tween frame model is locked in `tests/build-time/combat-chrome.test.ts`, and smoke/build plus the browser golden path pass without returning to the unstable CanvasEngine `animatedSignal` path.                                                                                                                                                                     |
| T2-03 | Damage-number pop-up labels                        | ✅     | Action-battle combatants render `poki-combat-feedback.ce` damage popups on HP drops through shared combat-object props, backed by the pure feedback model in `src/modules/main/combat-chrome.ts`. Wild encounters render `poki-wild-battle.ce` and update a visible `-N HP` popup on `utala`; the browser golden path asserts the popup before capture.                                                                                                                                                                                                                                                                                                                                       |
| T2-04 | Swap battler sprites to creature packs per species | ✅     | `src/config/creature-sprites.ts` now registers all 43 curated species spritesheets with action-battle-compatible `stand` / `walk` / `attack` fallbacks, including species whose authored sheet uses `idle_down` or walk-only strips. `src/modules/main/lead-battle-avatar.ts` syncs the server player's combat body, HP, ATK, and PDEF from the lead party creature; jan Ike, jan lawa, and green dragon set-piece intros activate the lead avatar and victory/death/map-change paths restore the field `hero` graphic. Unit tests lock the derivation/stat model, the integration rival test asserts `species_kon_moli` → `hero`, and the browser golden path asserts the same runtime swap. |
| T2-05 | Battler hit animation (shake + flash)              | ✅     | Action-battle already applies body flash/knockback on hits; `src/modules/main/combat-visuals.ts` now adds the combatant `hurt` body strip on HP drops, while `poki-combat-feedback.ce` keeps the sprite-local damage label/flash overlay. `tests/build-time/combat-visuals.test.ts` and `combat-chrome.test.ts` cover the wiring/model.                                                                                                                                                                                                                                                                                                                                                       |
| T2-06 | Faint animation (fade + slide)                     | ✅     | `poki-combat-faint.ce` is registered as a component animation and `playCombatFaintVisual()` emits a body-clone fade/drop before action-battle removes the defeated event. Jan Ike, all jan lawa, and the green dragon are wired; the dragon uses the dedicated `green_dragon_death` strip.                                                                                                                                                                                                                                                                                                                                                                                                 |
| T2-07 | Victory + level-up + XP gain sequence              | ✅     | `src/modules/main/victory-sequence.ts` preserves the pure ordering model, and `src/modules/main/victory-rewards.ts` now applies lead XP, persists the new level, and emits non-blocking XP / level-up / new-move notifications for wild catches, jan Ike, and jan lawa. Integration tests assert the jan Ike and jan Wawa notification path.                                                                                                                                                                                                                                                                                                                                                  |
| T2-08 | Defeat screen ("pakala! sina tawa ma tomo.")       | ✅     | Full-party player defeat opens the RPG.js-native `poki-defeat-screen` overlay before the revive dialog and last-safe-village respawn, with fallen/returning phases from `src/modules/main/defeat-screen.ts`, brand CSS in `src/styles/brand.css`, Vitest-safe no-op behavior for integration fixtures, and `tests/e2e/full/defeat-respawn.spec.ts` proving the browser fade plus safe-village return. If only the active battle lead faints and a bench creature still has HP, `onDead` now promotes that creature instead of respawning.                                                                                                                                                     |
| T2-09 | Catch UX: poki-throw animation + dialog            | ✅     | Wild encounters now render `throw`, `caught`, and `escaped` capture states through `poki-wild-battle.ce`: `poki` consumes the strongest carried poki item, plays the throw SFX, animates an arcing poki, then shows success/failure before the existing caught/escaped dialog. `tests/build-time/wild-battle-view.test.ts` locks the pure model and the full browser golden path asserts both throw and caught overlay labels.                                                                                                                                                                                                                                                                |
| T2-10 | Party Panel — open from field                      | ✅     | The HUD-native pause Party route renders up to six slots with creature portrait, TP-ish label, HP bar, and level; `tests/e2e/full/party-panel.spec.ts` proves it on an iPhone viewport after starter + catch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T2-11 | Party Panel — detail card (moves/XP/type)          | ✅     | Tapping a party slot opens a detail card with type, XP progress, XP-to-next, and known moves derived from the species learnset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T2-12 | Party Panel — reorder lead                         | ✅     | Pure helper `src/modules/main/party-order.ts` plus SQLite `setPartyOrder()` persist move-semantics; the Party detail card exposes a mobile-safe `lead` action that promotes the selected slot to slot 0. Drag-and-drop remains optional polish, not the current shipped contract.                                                                                                                                                                                                                                                                                                                                                                                                             |
| T2-13 | Item drops per species                             | ✅     | Species spine schema supports explicit `item_drop { item_id, chance, count }`, `build-spine` rejects drops that reference missing items, and `src/modules/main/item-drops.ts` now supplies a type/tier fallback loot table so every authored species resolves to a valid drop. Unit tests prove all 43 species have valid loot; real-engine/browser coverage still proves caught wild encounters can award inventory.                                                                                                                                                                                                                                                                         |
| T2-14 | `kili` healing item usable from party panel        | ✅     | SQLite `party_roster.current_hp` tracks per-creature HP, the Party detail card exposes a `kili` action for wounded selected slots, and `tests/e2e/full/party-panel.spec.ts` proves +20 HP plus inventory decrement for both lead and non-lead creatures on an iPhone viewport.                                                                                                                                                                                                                                                                                                                                                                                                                |
| T2-15 | "Items" submenu in combat action menu              | ✅     | Wild encounters now use `utala / poki / ijo / tawa`; `utala` lowers target HP for HP-scaled catch odds, `poki` consumes the strongest carried poki, and `ijo` opens a healing submenu for carried healing items such as `kili` / `telo_pona`, decrements inventory, and persists `party_roster.current_hp`. Covered by unit helpers, real-engine integration, the browser golden path, and the mobile Party-panel E2E slice.                                                                                                                                                                                                                                                                  |

---

## Phase 3 — Save, Menus, Transitions

**Goal:** real title screen, saves persist, transitions feel intentional.

| ID    | Title                                                                 | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | --------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T3-01 | Title scene — logo, new game / continue / settings / quit             | ✅     | The `rpg-title-screen` surface now has a branded parchment/sky logo treatment, stable New / Continue / Settings / Quit entries via `buildTitleMenuEntries()`, web quit-intent acknowledgement, and native Capacitor `App.exitApp()` wiring. `tests/build-time/title-menu.test.ts`, smoke, and `tests/e2e/full/continue-save.spec.ts` cover the row contract and web quit flow.                                                                                                                                                             |
| T3-02 | Autosave on map change                                                | ✅     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T3-03 | Autosave on combat end                                                | ✅     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T3-04 | Save on quit intent (beforeunload / resume)                           | ✅     | `src/modules/main/player.ts` flushes the slot-0 autosave in `onDisconnected`, covering browser unload, tab close, and native app background/disconnect through the engine signal without a separate client `beforeunload` listener.                                                                                                                                                                                                                                                                                                        |
| T3-05 | "Continue" boots into current map at player tile                      | ✅     | `tests/e2e/full/continue-save.spec.ts` now proves reload → continue prompt → restored map / tile / progression state in a real browser.                                                                                                                                                                                                                                                                                                                                                                                                    |
| T3-06 | Settings screen — sitelen, speed, contrast, accessibility, audio      | ✅     | Settings are reachable from the title and pause routes; text speed, high contrast, accessible mode, BGM volume, SFX volume, and sitelen-overlay preference persist through `src/platform/persistence/settings.ts`. `tests/e2e/full/accessible-mode.spec.ts` and `tests/e2e/full/text-speed.spec.ts` prove the highest-risk runtime settings in a real browser.                                                                                                                                                                             |
| T3-07 | Loading screen between map warps                                      | ✅     | Warps now open the RPG.js-native `poki-warp-loading` overlay around `changeMap()`, with enter/settle phases, destination labels from `src/modules/main/warp-loading.ts`, brand CSS in `src/styles/brand.css`, Vitest-safe no-op behavior for non-browser integration fixtures, and a full-browser golden-path assertion on the first unlocked warp.                                                                                                                                                                                        |
| T3-08 | New Game flow: confirm wipe if save exists                            | ✅     | `tests/e2e/full/continue-save.spec.ts` now proves existing-save prompt → cancel → confirm → wiped save state → starter reset in a real browser.                                                                                                                                                                                                                                                                                                                                                                                            |
| T3-09 | Pause menu — Resume / Party / Items / Save / Settings / Quit to title | ✅     | Dedicated `poki-pause-screen` GUI now ships with HUD-native Party / Vocab / Inventory / Bestiary / Settings routes, route content panes, footer Resume / Save / Quit-to-title rows, JSON-backed title/starter/pause/settings/inventory/save/vocabulary/party-panel/bestiary/quest/dialog/shop/wild-encounter copy/templates plus lead-movebar, dictionary export, and victory/item-drop/bench-switch templates in `src/content/gameplay/ui.json`, and browser coverage for touch-only open/quit/continue plus party detail/lead promotion. |
| T3-10 | Sync `currentMapId` + `player_tile` on every move                     | ✅     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T3-11 | Save schema versioning + migration                                    | ✅     | `PRAGMA user_version` tracked; `migrateSchema()` handles upgrades.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T3-12 | Credits scene on game clear                                           | ✅     | Minimal paged credits flow lands through `src/modules/main/credits-screen.ts` and fires immediately after green-dragon victory before the final autosave.                                                                                                                                                                                                                                                                                                                                                                                  |
| T3-13 | "Mastered words" screen                                               | ✅     | Pause → vocab shows mastered TP words with sightings count.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

---

## Phase 4 — Content Breadth

**Goal:** all 7 beats playable with biome-appropriate tilesets, the current four jan lawa set-pieces, badge UX, bestiary (no trademarked "dex" naming — "lipu soweli" in-game).

| ID    | Title                                                       | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | ----------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T4-01 | Tag each map spec with `biome` + `music_track`              | ✅     | All seven specs declare metadata; `scripts/map-authoring/lib/emitter.ts` and `scripts/map-authoring/lib/tmx-emitter.ts` write Tiled map-level properties; `src/content/map-metadata.ts` is the runtime BGM registry; `author:verify` now rejects `.tmj` drift as well as `.tmx` drift.                                                                                                     |
| T4-02 | `nasin_wan` biome=forest with forest_summer tileset         | ✅     | `scripts/map-authoring/palettes/forest.ts` + `nasin_wan.ts` now use the Fan-tasy `seasons` pack for grass, dirt trail, tall grass, trees, and bushes; `tests/build-time/map-spec-content.test.ts` prevents regression to core placeholder grass.                                                                                                                                           |
| T4-03 | `nasin_pi_telo` biome=water with water tileset              | ✅     | `scripts/map-authoring/palettes/water.ts` + `nasin_pi_telo.ts` now use the Fan-tasy `seasons` water, shore, road, and tall-grass tiles; the river is visible, the sandbar route/final trigger/NPC tiles remain walkable, and the water palette points at a Tiled collision tile.                                                                                                           |
| T4-04 | `ma_telo` biome=town (lake village) full paint              | ✅     | `scripts/map-authoring/palettes/lake-town.ts` + `ma_telo.ts` now paint a blocked lake, west/north causeways, stone plaza, houses, trees, shop stand, five NPC markers, and the gated north warp; sparse Fan-tasy image-collection tilesets are covered by parser/palette regression tests.                                                                                                 |
| T4-05 | `ma_lete` biome=ice with forest_winter tileset              | ✅     | `scripts/map-authoring/palettes/ice.ts` + `ma_lete.ts` now use the Fan-tasy `snow` pack for snow/ice ground, stone paths, snow tall grass, houses, trees, rocks, fenced garden props, five NPC markers, and the gated north warp.                                                                                                                                                          |
| T4-06 | `nena_sewi` biome=peak (mountain)                           | ✅     | `scripts/map-authoring/palettes/mountain.ts` + `nena_sewi.ts` now use Fan-tasy `seasons` highland grass, dirt/stone paths, encounter grass, gray rock-slope cliff tiles with Tiled collision, rock/tree landmarks, five reachable NPC markers, and the gated north warp.                                                                                                                   |
| T4-07 | `nena_suli` biome=cave                                      | ✅     | `scripts/map-authoring/palettes/cave.ts` + `nena_suli.ts` now use Fan-tasy `fortress` cave floors, rock-slope wall blockers with Tiled collision, cave encounter overgrowth, torch landmarks, five NPC markers, and the gated north warp to `nasin_pi_telo`; reduced-visibility shader remains optional polish.                                                                            |
| T4-08 | Warp graph connectivity test                                | ✅     | `tests/build-time/warp-graph.test.ts` — every map reachable from `ma_tomo_lili`.                                                                                                                                                                                                                                                                                                           |
| T4-09 | Jan lawa set-piece fights for current masters               | ✅     | All 4 shipped via gym-leader factory + BattleAi phase transitions.                                                                                                                                                                                                                                                                                                                         |
| T4-10 | Badge award dialog + persist `badges[]`                     | ✅     |                                                                                                                                                                                                                                                                                                                                                                                            |
| T4-11 | Badge display in pause menu                                 | ✅     | Inventory-screen renders earned badges.                                                                                                                                                                                                                                                                                                                                                    |
| T4-12 | Gate next-region warps until badge earned                   | ✅     | `warp.ts` `requiredFlag`.                                                                                                                                                                                                                                                                                                                                                                  |
| T4-13 | Bestiary screen — seen/caught grid/list                     | ✅     | Pause now has a `lipu` route that renders `lipu soweli` progress plus per-species rows with `jo` / `lukin` / `ala` tiers; `tests/e2e/full/party-panel.spec.ts` proves the route on an iPhone viewport after starter + catch.                                                                                                                                                               |
| T4-14 | Track `bestiary` seen/caught on encounter + catch           | ✅     | SQLite `bestiary_entries` persists earliest seen/caught timestamps, starter ceremony marks the chosen starter caught, wild encounters mark seen on appearance and caught on successful capture, and save/load runtime-state export/import round-trips the table. `tests/build-time/map-spec-content.test.ts` also locks that all 43 authored species appear in catchable encounter tables. |
| T4-15 | Authored multi-beat dialog per region (≥ 5 NPCs per region) | ✅     | All seven shipped maps now have at least five NPC markers; 17 new ambient NPCs have two-beat Tatoeba-validated dialog nodes, `src/modules/main/server.ts` runtime events, regenerated TMJ/TMX/previews, and `tests/build-time/map-spec-content.test.ts` coverage for the NPC floor plus dialog spine handoff.                                                                              |
| T4-16 | Shop NPC in `ma_telo` sells poki + kili                     | ✅     | `jan-moku` now runs a dialog-backed shop in `ma_telo` with `poki_lili`, `kili`, `telo_pona`, and `poki_wawa` stock, priced in `ma`; unit, integration, and full-browser golden-path tests prove purchase flow and inventory updates.                                                                                                                                                       |
| T4-17 | `ma` coin inventory + battle reward                         | ✅     | `ma` is authored as a stackable inventory item, rival/jan lawa/green-dragon battle victories grant `ma` through the SQLite inventory path, pause/detail inventory surfaces the count, and the real-engine/browser golden paths verify earned currency before spending it at jan Moku.                                                                                                      |

---

## Phase 5 — Audio, Mobile, Accessibility

**Goal:** feels good on phone, readable small, audible on speakers, passes basic accessibility.

| ID    | Title                                                                       | Status        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | --------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T5-01 | Biome → music track map                                                     | ✅            | `src/content/map-metadata.ts` maps each authored map to `biome` + ambient `music_track`; `src/content/gameplay/audio.json` owns BGM IDs, resolver paths, and combat-selection overrides, while `src/modules/main/audio.ts` keeps the pure `bgmForContext({mapId, inCombat, timePhase})` resolver and Pages/Capacitor-safe URL handling runs through `src/config/provide-audio-runtime.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T5-02 | Cross-fade music on map change                                              | ✅            | `src/config/audio-controller.ts` ships an 800 ms `BgmCrossfadeController`; the client runtime watches live RPG.js scene map data after the first user gesture and cross-fades through Howler. `tests/build-time/audio-controller.test.ts` covers track changes, same-track volume updates, stop scheduling, and `/` / `/poki-soweli/` / `./` asset bases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T5-03 | Combat music override                                                       | ✅            | Server cues emit `poki-soweli:audio:bgm-override` for jan Ike, jan lawa, and green dragon intro/defeat paths, action-key use near a live BattleAi event primes combat BGM plus hit SFX, and a per-player monitor watches `BattleAi.getTarget()` / `getState()` so AI-initiated alert/combat state also switches to combat BGM and returns to ambient when cleared.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T5-04 | Wire 12 SFX events                                                          | ✅            | `src/modules/main/sfx.ts` catalogs 12 event IDs, balanced base volumes, shipped `.ogg` resolver paths, and `effectiveSfxVolume(event, bus)`. Runtime cues now cover pause open, menu tick/confirm, footstep, warp, encounter appear, hit, catch throw/success/fail, level-up, and faint. Several IDs intentionally alias shared CC0 source files until a dedicated SFX pack lands; that is audio-polish work, not SFX-event wiring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T5-05 | Apply Settings volume sliders                                               | ✅            | Sliders persist in `src/platform/persistence/settings.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T5-06 | ~~Virtual d-pad for mobile~~                                                | ✅ superseded | Retired in favor of **tap-to-walk** per `docs/UX.md`. `src/modules/main/virtual-dpad.ts` + its 20 unit tests removed; the "which mental model drives mobile input" problem dissolves — we're not a handheld emulator.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| T5-07 | ~~Virtual A/B action buttons~~                                              | ✅ superseded | Retired per `docs/UX.md`. Interactions come to the player via a **contextual hint glyph** attached to the player sprite, not persistent on-screen buttons.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T5-08 | ≥ 44 dp touch targets audit                                                 | ✅            | `tests/e2e/full/mobile-touch-targets.spec.ts` audits title entries, starter dialog choices, HUD hamburger, contextual hint, and pause entries on an iPhone viewport. The audit caught the RPG.js dialog-choice rows at 36 px; `brand.css` now raises `.rpg-ui-dialog-choice` to the 44 px floor on coarse pointers. `tests/e2e/full/visual-audit.spec.ts` also emits mobile starter-choice artifacts and caught the unreadable inactive-choice contrast regression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T5-15 | **HUD `.ce` shell** — status strip + hamburger + hint glyph + pause overlay | ✅            | The HUD shell now ships as RPG.js-native CanvasEngine components: `poki-hud-status.ce`, `poki-hud-menu-toggle.ce`, `poki-hud-hint.ce`, and `poki-pause-screen.ce`. The pause overlay has HUD-native route tabs for Party / Vocab / Inventory / Bestiary / Settings, route content panes, and footer Resume / Save / Quit-to-title rows with the `pause-party`, `pause-vocab`, `pause-inventory`, `pause-bestiary`, `pause-settings`, `pause-save`, `pause-title`, `party-slot-{n}`, `party-heal-{n}`, and `bestiary-entry-{species}` test contracts. `tests/e2e/full/mobile-status.spec.ts`, `mobile-menu.spec.ts`, `mobile-hint.spec.ts`, `mobile-touch-targets.spec.ts`, and `party-panel.spec.ts` prove the shell and Party/Bestiary routes on an iPhone viewport.                                                                                                                                                                                                                     |
| T5-16 | Tap-to-walk + tap-to-interact                                               | ✅            | `src/config/provide-tap-controls.ts` now snaps taps onto the 32px player movement grid, resolves Tiled object rectangles before falling back to scene events so invisible warp hitboxes still route correctly, emits a server-authoritative route request, exact-lands on the requested tile, and auto-interacts when the target is an event. `tests/e2e/full/mobile-tap-controls.spec.ts` proves a real iPhone-viewport canvas tap lands on the requested tile before triggering `jan-sewi`, locked east-warp taps walk adjacent and surface the gated dialog instead of drifting to another interactable, and a rapid retap cancels the old route in favor of the latest destination. `mobile-hint.spec.ts` still proves both the adjacent-target hint glyph and direct player-sprite tap can trigger the same dialog, and that stray canvas taps are ignored while dialog is open. Follow-up browser work belongs to deeper touch failure-path depth, not the runtime contract itself. |
| T5-09 | ~~Portrait-lock for Android~~                                               | ✅ superseded | Retired because `docs/UX.md` requires responsive portrait and landscape play with **no orientation lock**. `@capacitor/screen-orientation` was removed, `cap sync android` now reports five plugins, `rg` finds no screen-orientation config/runtime/manifest lock, and `CAPACITOR=true pnpm build && pnpm exec cap sync android && cd android && ./gradlew assembleDebug` passes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| T5-10 | Text-speed slider applied to typewriter + combat log                        | ✅            | `src/config/rpg-dialog.ce` now reads the persisted `textSpeed` before each RPG.js dialog, maps `0` cps to instant text, and uses the same path for combat-log `player.showText` calls. `tests/build-time/typewriter-speed.test.ts` covers the cps-to-interval mapping; `tests/e2e/full/text-speed.spec.ts` proves instant and nonzero settings through the real title Settings dialog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| T5-11 | Color-contrast AA audit + high-contrast body class                          | ✅            | BRAND.md §Palette meets WCAG AA on body text; `poki-high-contrast` body class doubles borders + flattens gradients. Settings toggle wired.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T5-12 | "Accessible mode" toggle — larger fonts + reduced motion                    | ✅            | `src/platform/persistence/settings.ts` persists `accessibleMode`; `src/styles/brand-preferences.ts` applies `poki-accessible-mode`; `brand.css` raises text tokens and zeroes motion durations; `tests/e2e/full/accessible-mode.spec.ts` proves the Settings route toggles it in a real browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| T5-13 | Web ARIA landmarks + meta                                                   | ✅            | `index.html` now ships description/application/Open Graph metadata, a labeled `main` landmark, and a labeled RPG canvas host. `tests/e2e/smoke/boot.spec.ts` verifies the metadata and landmark contract in the real browser smoke path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| T5-14 | ~~Tutorial overlay on first boot~~                                          | ✅ superseded | Retired because `docs/DESIGN.md` explicitly says the game teaches through the world, not through a tutorial overlay. Current onboarding is starter ceremony + contextual hint glyph + tap-to-walk/tap-to-interact coverage rather than a modal first-boot lesson.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |

---

## Phase 6 — Release Hardening

**Goal:** CI matrix green, debug APK + web exports under budget, integration suite carries the real weight, and the next v0.2 release-please tag is published.

### Testing (parallel with runtime work — E2E-first policy per TESTING.md)

| ID    | Title                                                  | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | ------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T6-01 | Vitest unit project                                    | ✅     | `tests/build-time/**`; latest local unit + coverage run passed 761 tests with 1 skipped across 80 files. Coverage is 96.46% statements / 92.44% branches / 97.74% functions / 96.65% lines against a 95% statements / 90% branches / 95% functions / 95% lines gate.                                                                                                                                                                                                                                                                                                                                |
| T6-02 | Unit: schemas load all spine JSONs                     | ✅     | `schema-load.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T6-03 | Unit: build-spine emits expected shape                 | ✅     | `build-spine.test.ts` + cross-ref integrity + `PARTY_SIZE_MAX` spread.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T6-04 | Unit: encounter-roll weighted distribution             | ✅     | `encounter-roll.test.ts` — 10 k rolls within 3% of weights.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| T6-05 | Unit: type-matchup matrix                              | ✅     | `type-matchups.test.ts` — 25 matchups.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T6-06 | Unit: catch-math at full, half, 1HP                    | ✅     | `catch-math.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| T6-07 | Unit: XP curve + level-up boundaries                   | ✅     | `xp-curve.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| T6-08 | Integration: warp graph connectivity                   | ✅     | Topology test lives in unit (pure graph walk over built `world.json`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T6-09 | Integration: save round-trip                           | ✅     | `tests/integration/journey-golden-path.test.ts` drives real `player.save()` / `player.load()` through the engine and verifies external runtime-state restore (journey beat, party, inventory, flags, map).                                                                                                                                                                                                                                                                                                                                                                                          |
| T6-10 | E2E playthrough — integration suite (`@rpgjs/testing`) | ✅     | `tests/integration/journey-golden-path.test.ts` now covers the full region-progression chain in-engine: starter ceremony → warp → caught encounter → rival → four badge gates → green-dragon gate → `nasin_pi_telo` → green-dragon ending state / `game_cleared` / credits flow, plus respawn and save round-trip. In parallel, the local Playwright full suite now proves the full browser path from title → starter ceremony → first catch → rival → four badge gates → `nasin_pi_telo` → green-dragon clear → credits, and the mobile HUD suite proves party detail/lead promotion/`kili` healing. |

### Build & release

| ID    | Title                                                              | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ----- | ------------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T6-11 | Android signed release APK — release keystore path                 | ⬜     | Deferred beyond the current release contract. The current workflows intentionally publish debug APKs only; this item requires remote CI proof for keystore secrets, `assembleRelease`, GitHub release attachment, and physical-device QA before it can be marked done.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| T6-12 | Release-please: upload web + debug APK to GH release               | 🟡     | `release.yml` is wired to publish versioned workflow artifacts (`web-bundle-<tag>`, `android-debug-apk-<tag>`, `release-metadata`); `cd.yml` is wired to consume the completed release run, validate required metadata fields, attach artifacts to the GitHub release, and deploy Pages. `tests/build-time/workflow-contract.test.ts` pins the local config contract, including full-SHA action pinning across workflow files; `tests/build-time/release-artifacts.test.ts` and `pnpm release:smoke-artifacts "$RELEASE_TAG"` prove the local tarball/APK/metadata handoff shape and require the Pages tarball to contain deployed manifest/asset manifests plus runtime maps. Remote end-to-end proof remains T6-17. |
| T6-13 | Web bundle size audit                                              | ✅     | `tools/web-size-audit.mjs` + CI gate — dual-tier budget (40 MB hard fail / 10 MB target). Latest local audit: 8.32 MB gzip, under the target by 1.68 MB.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T6-14 | Asset compression pass (WebP, downscale)                           | ✅     | `scripts/deploy/prune-deploy-assets.mjs` runs after `vite build`, preserves authoring fixtures in `public/`, and rewrites deployable `dist/assets/tilesets/` to the runtime allowlist derived from `src/tiled/*.tmx`. This dropped the audited bundle from 26.97 MB gzip to 8.32 MB gzip with favicon/manifest assets included.                                                                                                                                                                                                                                                                                                                                                                                       |
| T6-15 | Expand CI matrix                                                   | ✅     | `ci.yml`: `unit` → `integration` + `e2e-smoke` → `build` → `android-debug-apk`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T6-16 | Commitlint gate on PR title + commits                              | ✅     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T6-17 | Write CHANGELOG prelude + tag next v0.2 release via release-please | ⬜     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| T6-18 | Smoke-test debug APK on physical Android device + iOS browser      | 🟡     | `docs/RELEASE_QA.md` manual checklist plus `.maestro/` Android debug-APK and iOS Safari Pages flows. `pnpm maestro:check` validates the checked-in flow syntax locally; actual Android emulator execution, iOS simulator execution, and physical-device release-attached APK/browser QA remain open. Signed-release physical QA belongs to T6-11 when that track is enabled.                                                                                                                                                                                                                                                                                                                                          |

---

## Phase 7 — Post-v0.2 Depth + Replay Value

Turns the 15-min slice into a ~4-hour game.

| ID    | Title                                                                         | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----- | ----------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T7-01 | Expand journey: beats 8-10 (Hall of Masters rematches + endless-dungeon hook) | 🟡     | Pure `src/modules/main/rematch.ts` covers status, scaled XP/level, cycling drop table, and cooldown math. Beat 8-10 map specs and runtime entry points still need to be authored before this is observable in play.                                                                                                                                                                                                                                                                                          |
| T7-02 | 20 new species (43 → 63)                                                      | ⬜     | ≥ 3 unique per biome.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| T7-03 | 15 new moves (17 → 32) covering type × tier matrix                            | ⬜     | Every type has lili/wawa/suli + 1 signature; learnset reshuffled.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| T7-04 | Status effects: `seli` burn, `telo` wet, `lete` frozen                        | 🟡     | `src/content/gameplay/combat.json` owns status chances, durations, tick effects, and damage multipliers; `src/modules/main/status-effect.ts` keeps the pure resolver. Combat runtime integration and visible feedback are still open.                                                                                                                                                                                                                                                                        |
| T7-05 | Breeding / nurturing: daycare at village                                      | 🟡     | `src/content/gameplay/progression.json` owns offspring level/suffix, stat jitter/clamps, learnset windows, and type-inheritance overrides; `src/modules/main/daycare.ts` keeps pure derivation helpers. The daycare NPC/menu flow is still open.                                                                                                                                                                                                                                                             |
| T7-06 | Treasure chests scattered across all 7 maps                                   | 🟡     | `src/modules/main/treasure-chest.ts` covers weighted loot plus flag/mastered-word gating. Map placement, runtime event wiring, and browser proof are still open.                                                                                                                                                                                                                                                                                                                                             |
| T7-07 | Ambient world events (weather, day/night cycle, festivals)                    | 🟡     | `src/modules/main/ambient-events.ts` covers day/night plus per-biome weather. Runtime tint, weather presentation, and dialog-variant binding are still open.                                                                                                                                                                                                                                                                                                                                                 |
| T7-08 | Side quest framework: 3-step fetch / defeat / deliver                         | ✅     | `src/modules/main/quest.ts` defines 4 goal kinds and the current 7-map quest catalog; `src/modules/main/quest-runtime.ts` persists active/completed state through flags, catch/defeat/delivery hooks advance progress, `QuestNpc()` wires quest-giver NPCs, and the HUD-native Inventory route lists accepted/completed quests. `tests/e2e/full/side-quest.spec.ts` proves the mobile forest-quest acceptance/progress/reward path. Deeper post-game quest chains remain future content, not a v0.2 blocker. |
| T7-09 | Secret area: underwater route after `badge_telo`                              | ⬜     |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| T7-10 | Post-game New Game Plus with party carryover                                  | 🟡     | `src/content/gameplay/progression.json` owns NG+ unlock/reset/scaling/reward rules; `src/modules/main/new-game-plus.ts` keeps save derivation + carryover behavior. Menu wiring and end-to-end play proof are still open.                                                                                                                                                                                                                                                                                    |

---

## Phase 8 — Language Learning Layer

Explicit affordances the player can OPT INTO without breaking the no-translation rule.

| ID    | Title                                                  | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----- | ------------------------------------------------------ | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T8-01 | `lipu nasin` — personal sentence log                   | ✅     | Dialog playback now records generated TP beats into the v6 `sentence_log` SQLite table via `src/platform/persistence/queries.ts`, runtime save/import/export carries the log, the pause Vocab route exposes a tap target that opens a TP-only sentence-log dialog, and `tests/e2e/full/mobile-menu.spec.ts` proves the iPhone tap path.                                                                                                                                                                                                                                                                                                                                                   |
| T8-02 | Per-species description re-read from `lipu soweli`     | ✅     | Seen/caught `lipu soweli` rows are tap targets; `src/modules/main/bestiary-panel.ts` pulls generated TP species descriptions into JSON-templated reread text, `src/modules/main/pause-menu.ts` opens the normal dialog bubble, and `tests/e2e/full/party-panel.spec.ts` proves the iPhone tap path.                                                                                                                                                                                                                                                                                                                                                                                       |
| T8-03 | Word-card hover: sitelen-pona glyph + sightings count  | ✅     | The mobile-first replacement is tap/click, not hover-only: mastered words in the pause Vocab route render a sitelen glyph prefix plus sightings count, tapping a row opens a glyph/word/sightings dialog card, the copy/templates live in `src/content/gameplay/ui.json`, and `tests/e2e/full/mobile-menu.spec.ts` proves the iPhone tap path.                                                                                                                                                                                                                                                                                                                                            |
| T8-04 | Optional "show me the TP sitelen" toggle during dialog | ✅     | The Settings `sitelen` toggle now drives `rpg-dialog.ce`: when enabled, the dialog renders a stable `dialog-sitelen-overlay` glyph line from `src/styles/sitelen-overlay.ts`, with unknown/non-TP words skipped. `tests/e2e/full/text-speed.spec.ts` proves the live browser dialog path.                                                                                                                                                                                                                                                                                                                                                                                                 |
| T8-05 | Tatoeba-backed "wan sitelen" micro-game                | ✅     | The micro-game data now lives in validated `src/content/gameplay/language.json`; `src/modules/main/micro-game.ts` runs deterministic pick-the-TP-sentence rounds through RPG.js `showChoices`, the pause Vocab route exposes the tap target, and `tests/e2e/full/mobile-menu.spec.ts` proves the iPhone choice-dialog binding.                                                                                                                                                                                                                                                                                                                                                            |
| T8-06 | Export personal dictionary to shareable card           | ✅     | `src/modules/main/dictionary-export.ts` renders deterministic text + SVG from the validated `dictionary_export` section in `src/content/gameplay/ui.json`; `src/config/provide-dictionary-export.ts` receives the RPG.js `player.emit` payload after map-room reconnect and uses Web Share when available, clipboard text copy when permitted, and SVG download as the hard fallback. Focused unit tests cover the text card, SVG card, sort/limit rules, empty state, repeat-clear badge, XML escaping, persisted mastered-word snapshot, and server emit/dialog wiring; `tests/e2e/full/mobile-menu.spec.ts` proves the iPhone Vocab → `lipu nimi` tap path and browser export payload. |

---

## Phase 9 — Engine, Perf, Infrastructure

Runs smoothly on a 3-year-old phone and is pleasant to develop.

| ID    | Title                                                       | Status        | Notes                                                                                                                                                                                                                                                                                                                                  |
| ----- | ----------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T9-01 | Asset WebP conversion pipeline                              | ⬜            | All `public/assets/**.png` have `.webp` sibling; vite plugin serves WebP to capable clients.                                                                                                                                                                                                                                           |
| T9-02 | Sprite-sheet atlas packing                                  | ⬜            | 70+ PNG sheets → ≤ 10 atlases.                                                                                                                                                                                                                                                                                                         |
| T9-03 | Audio dynamic-import (don't ship all bgm in initial bundle) | ⬜            | Only active-biome BGM loaded on boot.                                                                                                                                                                                                                                                                                                  |
| T9-04 | Vitest coverage reporting + CI gate                         | ✅            | 95%/95%/90%/95% ratcheted (v8 provider, `lcov` artifact, text-summary).                                                                                                                                                                                                                                                                |
| T9-05 | Benchmark suite for hot paths                               | ⬜            | `pnpm bench` + CI alerts on regressions > 20%.                                                                                                                                                                                                                                                                                         |
| T9-06 | Visual regression — every map → PNG + pixel-diff            | ✅            | `tests/build-time/map-preview-regression.test.ts` exact-pixel-diffs all seven committed map preview PNGs against freshly rendered TMJ output, and `tests/e2e/full/visual-audit.spec.ts` attaches live browser canvas screenshots for every authored map in addition to title/mobile-HUD artifacts. Latest focused runs passed locally. |
| T9-07 | Integration test harness extensions                         | ✅ superseded | Subsumed by T6-10 — `@rpgjs/testing` is the new harness.                                                                                                                                                                                                                                                                               |
| T9-08 | Release-please auto-rebase of pending PRs on main           | ⬜            |                                                                                                                                                                                                                                                                                                                                        |
| T9-09 | Sentry / error tracking integration                         | ⬜            | Respects kid-audience PII constraints.                                                                                                                                                                                                                                                                                                 |
| T9-10 | Bundle analyzer integration                                 | ⬜            | CI comments PR with top-10-largest modules delta vs main.                                                                                                                                                                                                                                                                              |

---

## Phase 10 — Platform Expansion

Beyond web + Android debug.

| ID     | Title                                         | Status | Notes                                           |
| ------ | --------------------------------------------- | ------ | ----------------------------------------------- |
| T10-01 | iOS Capacitor shell + signing                 | ⬜     | `cap add ios` + TestFlight.                     |
| T10-02 | Play Store production listing                 | ⬜     | Screenshots, description, free pricing.         |
| T10-03 | Localized store copy (Spanish + French)       | ⬜     | TP name universal; store copy translated.       |
| T10-04 | Windows / macOS desktop via Electron or Tauri | ⬜     | Same save-state portability.                    |
| T10-05 | WebXR stub for Quest browser                  | ⬜     | Renders in Quest browser; no controller rebind. |

---

## RPG.js v5 — framework-specific items

| ID    | Title                                                     | Status        | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----- | --------------------------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V5-01 | Inspector surface for E2E                                 | ✅ superseded | Not needed — `@rpgjs/testing` gives in-process engine access directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| V5-02 | Multi-creature party combat beyond the lead-avatar bridge | 🟡            | The current v0.2 bridge makes action-battle set-pieces visibly and mechanically use the lead creature as the combat avatar: species graphic, HP, SP, ATK, PDEF, and movebar model come from `party_roster[0]` + generated species/move data. The pause Party route can promote a new lead during an active battle, `onDead` auto-promotes the next conscious creature instead of respawning when the active lead faints, the movebar exposes a persistent in-combat switch row for conscious bench creatures, and configured rival/jan lawa/green-dragon targets render a cyan sprite-local reticle. Still open: a richer full-party battle command flow beyond direct lead switching, including explicit target selection if future set-pieces add multiple simultaneous enemies.                                                                                                                                                                                                                                                                                                                         |
| V5-03 | Moves + cooldown UI (vs the module's hotbar)              | ✅            | `lead-battle-skills.ts` maps generated move records and species learnsets into RPG.js skill database entries and a custom movebar model, `poki-lead-movebar.ce` exposes up to four lead moves with SP/type/power/cooldown metadata, a target/range pill, and a JSON-copy-backed bench switch row, and `poki-combat-target-reticle.ce` marks the configured live combat target in the canvas only while the lead movebar GUI is displayed. Browser/full + unit coverage verifies the bridge avoids RPG.js beta's learned-skill array hydration bug. `tests/e2e/full/action-battle-lead-movebar.spec.ts` now runs on an iPhone viewport, catches a bench creature, captures the single-target cyan reticle screenshot with a pixel assertion, taps the in-combat switch row, proves party order and battle sprite update, then taps the switched lead's move in live jan Ike combat and proves SP spend, BattleAi HP damage, target state, 44px+ target sizing, and cooldown/ARIA DOM state. No gameplay action is keyboard-only; optional gamepad parity remains future polish, not a current v0.2 blocker. |

---

## Risks

1. **RPG.js v5 is beta.** API churn possible between `5.0.0-beta.1` and stable. _Mitigation:_ pin the version; human review on every `@rpgjs/*` Dependabot PR; integration suite catches API breakage immediately (by failing to boot).
2. **Android keystore & signing.** Signed release APK production is intentionally deferred; enabling it later still depends on correctly provisioned repository secrets and remote CI proof. _Mitigation:_ keep debug APKs as the current release artifact, then reopen T6-11 only after generating `release.keystore` locally outside the repo, uploading base64 + passwords/alias to GH secrets, and proving `assembleRelease` plus release attachment in GitHub Actions.
3. **Web export > 10 MB regression.** The target now passes because deploy builds prune unused Fan-tasy source/sample tileset assets. _Mitigation:_ keep `tools/web-size-audit.mjs` in CI and keep `scripts/deploy/prune-deploy-assets.mjs` tied to the runtime map `.tmx` allowlist.
4. **Tatoeba corpus drift.** Corpus updates weekly; future refresh could invalidate passing lines. _Mitigation:_ corpus is vendored (`src/content/corpus/tatoeba.json`), refreshed only on human command via `pnpm fetch-corpus`, never in CI.
5. **Save schema churn during Phase 4-5.** Adding bestiary fields, badges, `ma` coins grows save payload. _Mitigation:_ T3-11 schema versioning is live (`PRAGMA user_version` + migrations); every additive change bumps `DB_VERSION`.
6. **Action-battle → party combat depth.** The v0.2 bridge now swaps the player body/stats to the lead creature, exposes generated lead moves through a custom cooldown movebar, supports both pause-route and in-combat lead switching, auto-promotes the next conscious creature after lead faint, and marks configured live targets with a sprite-local cyan reticle. The upstream action-battle model still does not provide a richer full-party command layer or explicit multi-target selector. _Mitigation:_ V5-02 remains partial while keeping tap/click as the primary contract.
7. **Mobile input vs grid-stepped movement.** RPG.js v5 controller is grid-snapped; a thumbstick wants analog. _Mitigation:_ tap-to-walk instead of virtual stick (per `docs/UX.md`) — the tap target is a tile, and the engine pathfinds one grid step at a time. Keyboard remains the desktop shortcut path. `virtual-dpad.ts` retired as of 2026-04-20.
8. **Integration-suite singleton hazards.** RPG.js engine uses module-level singletons; parallel test files trample state. _Mitigation:_ `fileParallelism: false` on the integration project in `vitest.config.ts`, `afterEach(clear)` in every test. Accepts slower CI for correctness.

---

## Parallelization & ownership

-   **Content authoring** — each map spec or dialog JSON is independent. Worktree-per-agent, max 5 in flight (per AGENTS.md).
-   **Integration tests** — one feature per file (singletons); but every feature gets one.
-   **Runtime UI wiring** — T5-15 HUD shell is landed; T2-01 combat chrome is landed for the action-battle hotbar; T2-02 is done with live combatant HP bars plus requestAnimationFrame width/color tweening; T2-03 is done for action-battle and wild encounter damage popups; T2-04 is done for lead-creature action-battle body/stat swaps; T2-08 is done for the branded full-party defeat/respawn overlay; T2-09 is done for wild capture throw/result feedback; T2-05/T2-06 now have body-level hit/faint visuals; T3-07 is done for branded warp loading overlays; and V5-03 is done for the browser-proven lead movebar/cooldown/switch/target-reticle layer. V5-02 now covers pause-route lead promotion, in-combat switch-row lead promotion, and automatic next-creature send-out; deeper full-party battle command polish remains open. Tap-to-walk is proven locally through the real mobile browser path and credits land through the server-driven overlay path.
-   **Engine-serial** — anything touching `src/modules/main/server.ts`, `player.ts`, or `src/config/config.client.ts` runs serially.

---

## Completion forecast

Aggressive estimate with 1 engineer + opportunistic parallel agents (content / UI), starting from the current state:

| Phase | Weeks | Critical path                                                                                         |
| ----- | ----- | ----------------------------------------------------------------------------------------------------- |
| 2     | 0     | Complete for the current v0.2 combat polish contract; deeper V5-02 party switching is post-v0.2 depth |
| 3     | 0     | Complete for the current save/menu/transition contract                                                |
| 4     | 0     | Complete for the current seven-map v0.2 content floor                                                 |
| 5     | 0     | Complete for the current mobile/audio/accessibility contract; unique audio-source polish is follow-on |
| 6     | 1     | T6-17 release-please/CD proof → T6-18 emulator/simulator + physical-device QA                         |

**Current v0.2 ETA:** intentionally unset. The remaining release-proof and device-QA work is a larger source of schedule risk than any single calendar estimate.

**Critical path:** Phase 6's remaining release blockers are T6-17 (release-please tag flow with `CI_GITHUB_TOKEN_PAT` plus the `release.yml` → `cd.yml` artifact handoff) and T6-18 (run the checked-in Maestro flows on Android/iOS emulators, then physical-device debug APK smoke plus iOS browser smoke). T6-11 signed release APK support is deferred outside the current release contract. The golden-path test suites are no longer the blocker: integration already covers the full engine path including combat `utala / poki / ijo / tawa`, bestiary writes, battle-earned `ma`, and jan Moku shop purchases; the local browser suite now covers the full clear path through credits including the `ma_telo` shop stop; and the mobile browser suite covers the HUD `lipu soweli` route, Party detail/lead-promotion, in-combat movebar lead switching, sprite-local target-reticle capture, `kili` in both combat and Party contexts, and the forest side-quest HUD inventory/reward path. Further browser/device hardening is follow-on work, not the first gate.

---

## Micro backlog (< 1 hr each)

Low-risk polish. Pick when blocked on larger work.

-   [x] Drive NPC + warp coordinates in `src/modules/main/server.ts` from the compiled `world.json` map-object layer instead of hand-copying pixel coords. `build-spine` now emits map objects into `world.json`, `events.json` carries behavior payloads plus intentional offsets only, and `runtime-map-events.ts` resolves event positions/warp targets from map specs.
-   [x] Tighten `IPreferences` in `src/platform/persistence/preferences.ts` to `PreferenceKey` for callers that control the key; keep raw `string` keys only at the `PreferencesAdapter` boundary. `runtime-state.ts` now guards imported preference keys before restoring them, and `tests/build-time/preferences.test.ts` locks the boundary.
-   [x] Add `poki soweli` favicon (16/32/48/192/512 PNGs + manifest.json). `public/icons/poki-soweli-{16,32,48,192,512}.png`, `public/manifest.json`, `index.html` icon links, smoke assertions, and `tests/build-time/favicon-manifest.test.ts` now lock the install/favicon contract.
-   [x] Promote selected Playwright map screenshots into `docs/screenshots/` for README/gallery use. `docs/screenshots/visual-audit/` now carries stable copies of the title, starter map, mobile starter dialog, mobile pause HUD, and all seven authored live map-canvas captures; `visual-audit.spec.ts` also rejects idle-map combat-reticle leakage while the lead movebar is closed.
-   [x] Add a `--dry-run` flag to `scripts/map-authoring/cli/all.ts`. `pnpm author:all --all --dry-run` now validates, emits TMJ/TMX in memory, renders previews from temporary TMJ files, and leaves committed map artifacts untouched.
-   [x] Rename `src/modules/main/index.ts` → `main.ts`. Server and client config now import `provideMain` explicitly from `src/modules/main/main.ts` instead of relying on a directory barrel.
-   [x] Dedupe `catch_rate: 0.18` magic numbers between species JSONs. Verified with `rg`: only `src/content/spine/species/seli_moli.json` still carries `0.18`, so the duplicate magic-number condition no longer exists.
-   [x] Add README badge: "43 species · 7 regions · 4 masters". `README.md` now carries shields for 43 species, 7 regions, and 4 region masters.
-   [x] Add `dialog_not_found` fallback that renders the dialog id (authoring-miss visibility). `playDialog()` uses the JSON-backed `dialog.missing_node_template`, and `tests/build-time/dialog-content.test.ts` proves a missing id renders visibly.
-   [x] Make vocabulary-screen pagination configurable (PAGE_SIZE const → preference). `ui.json` owns `vocabulary.page_size`, the schema validates it, `VOCABULARY_SCREEN_CONFIG.pageSize` drives `showVocabulary()`, and `gameplay-config.test.ts` locks the value.
-   [x] Add `--verbose` to `pnpm build-spine` for per-file stats. `pnpm build-spine -- --verbose` now reports each spine JSON file's bucket/count and each compiled TMJ map's object count while preserving the default concise output.
-   [x] Verify inventory-screen handles party size 0 gracefully. `showInventory()` returns the JSON-backed empty-party line without rendering a misleading `0 / 6` roster, and `tests/build-time/inventory-screen.test.ts` protects it against regressions.
-   [x] Run `prettier --write` on `src/` as a single chore: formatting pass. `prettier@3.3.3` is pinned, `pnpm format:src` formats supported TS/JSON/CSS/MD files under `src/`, `pnpm format:src:check` verifies the contract, and `.prettierignore` excludes generated maps/world, vendored corpus JSON, and CanvasEngine `.ce` components.
-   [x] Add `.editorconfig` for non-VSCode editors. The repo root now defines LF/UTF-8/final-newline defaults, 4-space source indentation, 2-space JSON/YAML indentation, Markdown trailing-whitespace preservation, and Makefile tabs.

## Macro vision (unscheduled)

Aspirational arcs that inform direction but aren't planned for the current v0.2 milestone:

-   **Seasons of the Seven Regions** — live content drops, new boss per quarter.
-   **Community-authored journey packs** — user-submitted `.json` spine overlays.
-   **AI dialogue companion** — local LLM refines jan Sewi's hints per player vocab stats (opt-in, no network).
-   **Classroom mode** — multi-student teacher dashboard tracking TP word mastery across a class of tablets.
-   **Soweli trading** — offline P2P QR-code creature trades at the village square.
-   **Anthropic showcase build** — `claude.ai/code` demo of Claude-assisted game dev end-to-end.

---

## How to read this doc

A status mark is `✅` only when the behavior is **observable in a playtest** or the original requirement was explicitly superseded by a proven replacement, not when a code review approves pure wiring. `🟡` means the contract is partially met but runtime wiring or polish is below the DoD. `⬜` means nothing has landed yet.

When adding a task, append it to the relevant phase's table with the next sequential ID. Don't rewrite historical IDs — they're referenced from git history + PRs.

When marking a task done, the commit that closes it cites the ID in the message body (`T2-07`, `T4-14`, etc.) and this table flips from `🟡` or `⬜` to `✅`. The commit also tells you which of the linked file paths changed.
