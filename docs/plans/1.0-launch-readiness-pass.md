---
title: 1.0 Launch Readiness Pass
updated: 2026-04-23
status: active
domain: planning
---

# 1.0 Launch Readiness Pass

Evidence-annotated walk of `docs/LAUNCH_READINESS.md` against the verified state at `v0.3.1`. Each row is either **proven** (with evidence), **pending** (with owner + gate), or **out-of-scope for v1** (with rationale).

This is not the canonical checklist. `docs/LAUNCH_READINESS.md` is. This is a *current-audit snapshot* to update as work lands.

## Product

- [ ] **Fresh-start to credits is playable without doc lookup.**
  - Proven: starter → four region masters → green dragon → credits path exists and runs in-engine.
  - **Pending:** a fresh-eyes playthrough where a non-developer reaches credits without docs. Required evidence: recorded session + notes in `docs/visual-audit/1.0-playthrough/`.

- [ ] **Quest flow has no known softlock.**
  - Proven: integration + e2e golden-path specs cover the main arc.
  - **Pending:** three side quests walked by a human per region after the quest-content buildout (T7 in the batch).

- [ ] **Required region-master progression is legible.**
  - Proven: four masters wired, badge gating logic covered by unit + integration.
  - **Pending:** visual-audit screenshots of each master dialog confirm the player reads "what to do next" without guessing.

- [ ] **Save/Continue works from a real release artifact.**
  - Proven: integration round-trip covered; local release artifact contract test covers smoke.
  - **Pending:** manual test against the release-attached web tarball + APK (not a local build).

## World And Visuals

- [ ] **`docs/VISUAL_REVIEW.md` checklist has been walked for the current branch.**
  - **Pending:** run visual-audit spec + curate screenshots + walk VISUAL_REVIEW for the 1.0 branch cut.

- [ ] **All authored maps have current visual-audit screenshots.**
  - Proven: `docs/screenshots/visual-audit/` has 12 current PNGs (desktop title, desktop starter canvas, mobile starter choice, mobile pause, 7 maps, 1 extra).
  - **Pending:** refresh after map recomposition (T10 in the batch) lands.

- [ ] **No known blocker/overlay/transition issue is being silently accepted.**
  - Proven: author:verify + preview-regression tests guard the known categories.
  - **Pending:** visual audit review of the recomposed maps.

## Release And Platform

- [x] **Latest remote `release.yml` artifact run succeeded.**
  - **Proven:** run `24819206623` produced `rivers-reckoning-web-v0.3.1.tar.gz` and `rivers-reckoning-v0.3.1-debug.apk`.

- [x] **Latest remote `cd.yml` workflow-run deploy succeeded.**
  - **Proven:** run `24819295738` consumed the `release.yml` workflow_run and deployed Pages.

- [x] **GitHub release has both the web tarball and debug APK attached.**
  - **Proven:** `gh release view v0.3.1` shows both.

- [x] **Pages boot verified at `https://arcade-cabinet.github.io/poki-soweli/`.**
  - **Proven:** asset-load audit (`tests/e2e/full/asset-load-audit.spec.ts`) against `GITHUB_PAGES=true pnpm preview` shows 48 requests / 0 failures / 0 console errors / 0 page errors. Same bundle is what deploys.

## Mobile

- [x] **Android emulator smoke completed with the release-attached debug APK.**
  - **Proven:** `pnpm maestro:android` passed on `Maestro_ANDROID_pixel_6_android-33` per STATE.md.

- [ ] **One physical Android device smoke completed with the release-attached debug APK.**
  - **Pending:** T13 in the batch. Requires physical device access.

- [ ] **iOS simulator or Mobile Safari Pages smoke completed against deployed Pages.**
  - **Pending:** T12 in the batch. Requires macOS + simulator.

- [ ] **Portrait and landscape layouts remain usable.**
  - **Pending:** visual-audit pass on mobile viewport in both orientations.

## Docs

- [x] **`docs/STATE.md` matches the current shipped truth.**
  - **Proven:** STATE.md updated on 2026-04-23 to reflect `v0.3.1` state. Residual Naming section added via PR #94. Full-browser test count aligned with current file set via PR #95.

- [x] **`docs/PRODUCTION.md` matches the actual remaining work.**
  - **Proven:** PRODUCTION.md covers pillar-level gaps; updated alongside STATE.md.

- [ ] **`docs/ROADMAP.md` statuses match the verified state.**
  - **Pending:** pending PRs (#93, #97) bump phase counts and add rows for PR #81 salvage + art direction decision. When they land, walk ROADMAP.md top-to-bottom and reconcile status ticks against reality.

- [x] **Root docs (`README.md`, `CLAUDE.md`, `AGENTS.md`, `STANDARDS.md`) align with the same story.**
  - **Proven:** all four carry the current STATE.md baseline date per the `docs-current-state.test.ts > keeps root onboarding docs aligned with the STATE baseline date` guard.

## Summary

| Category | Status |
| --- | --- |
| Release and platform | 4/4 proven |
| Docs alignment | 3/4 proven (ROADMAP pending PR merges) |
| Product | 0/4 proven (all need a human playthrough pass) |
| World and visuals | 0/3 proven (all need post-recomposition audit) |
| Mobile | 1/4 proven (physical device + iOS pending) |

**Distance to v1.0 (rough estimate):**
- 3 batch-PR passes away (quest content → map recomposition → audio pass)
- 2 hardware days away (physical Android + iOS smoke)
- 1 full manual playthrough away
- 1 final docs reconciliation pass

## Adjacent gaps not on the checklist

These are product-quality items that LAUNCH_READINESS doesn't call out but would block a polished v1:
- **Side-quest density.** Current arc is the four-master gate loop; side content is thin. Story bible (PR #96) names the target; T7 in the batch implements.
- **Combat and economy tuning.** Numbers are playable, not fun-tuned. T8 in the batch.
- **Audio identity.** Kenney placeholders work but don't earn their keep. T11 captured the gap (PR #98).
- **Post-clear loop.** Story bible names free-exploration; runtime implementation is T14.
