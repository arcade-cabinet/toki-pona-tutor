name: CD

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: |
          echo "CD placeholder — validation already gated in ci.yml."
          echo "Real deploy steps (Pages, APK upload, etc.) land with T6-11/T6-12."
